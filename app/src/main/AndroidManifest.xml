<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminDashboard"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppUpdateCenterActivity" android:exported="false" />

        <!-- 🛠️ Admin Tools — كل واحدة بصفحة مستقلة (الإصلاح الكارثي) -->
        <activity android:name=".SanitizerActivity" android:exported="false" />
        <activity android:name=".CategoryVisibilityControlActivity" android:exported="false" />
        <activity android:name=".XtreamTesterActivity" android:exported="false" />
        <activity android:name=".GeminiAssistantActivity" android:exported="false" />
        <activity android:name=".AddUserActivity" android:exported="false" />
        <activity android:name=".MasterLinkActivity" android:exported="false" />
        <activity android:name=".AddMacActivity" android:exported="false" />
        <activity android:name=".CodemagicCenterActivity" android:exported="false" />

        <!-- 🧰 Legacy pages -->
        <activity android:name=".AdminActivity" android:exported="false" />
        <activity android:name=".ReportActivity" android:exported="false" />
        <activity android:name=".ServerListActivity" android:exported="false" />
        <activity android:name=".UserListByServerActivity" android:exported="false" />
        <activity android:name=".UserDetailControlActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.latchi.admin.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
